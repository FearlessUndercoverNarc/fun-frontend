export interface Board {
    title: string,
    description: string,
    authorAccountId: number,
    authorAccountFio: string,
    parentId: number,
    parentTitle: string,
    createdAt: string,
    lastUpdatedAt: string
}